# 价目表 · 用户逻辑与目标模型设计方案

> 版本：V1.0（2026-08-31，待产品评审）
> 性质：概念模型与整改方向的拍板文档。评审通过后，由本文件派生页面级整改清单与原型实施方案。
> 文档来源：
> - 《价目表操作说明和整改方案（优化版）.docx》（仓库 `文档/`，旧系统真实用法与整改点一/二）
> - 《## 价目表优化整改方案.md》（Obsidian，基于研发检视意见的 26 条页面整改 A1-A11 / B1-B10 / C1-C5）
> - 原型代码现状（`业务分析与设计准备/src/pages/PriceConfig.tsx` 等的探索结论）

---

## 1. 结论先行

价目表当前的"混乱且没有逻辑"不是页面级问题，而是三个层面的结构性问题：

1. **信息架构按数据库表组织**：用户面对的是 `product_budget` / `product_budget_ext` / `product_budget_report` 三张表的投影（"第二套品种价目表及比例配置"这种命名即证据），而不是"我药厂的价格怎么配"；
2. **对公/对私生效规则是隐式矩阵**：药厂开关「公私合一」三态 × 两套表 × 两档版本，用户必须背下来；
3. **【原型特有·最重】配置页与算费体系平行断裂**：`PriceConfig.tsx` 用本地 string 模型自成一派，任务创建读的是 `seedPriceBooks` 写死的 4 条价格定义——在价目表页改任何价格，创建任务的算费纹丝不动；对私金额流不到绩效、区域价流不到填报、发包标记不决定发包取价。

**目标模型（已拍板）一句话**：菜单收敛为「价目管理 → 价目表配置」唯一入口，列表只看 品种×药厂×绑定价目表；价目表是独立主数据（一张表、四个价格区），药厂绑默认表、品种继承、特殊品种解绑成副本；对公/对私由药厂开关显式驱动。

---

## 2. 用户逻辑还原（旧系统真实用法）

价目表的本质是一张 **「服务项 × 定价维度」的价格矩阵 + 一套取价规则**。它服务四条业务主线：

| 主线 | 谁用 | 用什么字段 | 什么时候用 |
|---|---|---|---|
| **发包线** | 药厂创建任务 | 比例表 + 「是否发包金额」（每业务组唯一，决定发包取哪个价） | 建任务时按比例分配金额、换算数量 |
| **填报线** | 服务专员日常填报 | 金额；**区域模式下按城市线级取价**（一线~五线 + 对私线级共 12 列） | 拜访 / 会议填报计价 |
| **绩效线** | 服务商给代表打绩效 | **对私金额 / 对私超员金额 / 对私城市线级** | 公私分离场景 |
| **效率线** | 运营接新品种 | 价目表复制 / 比例表复制 / 批量复制 / 导入导出 / 医院拜访同类项复用 | 品种多、逐格改价慢，**复制与导入导出才是主路径** |

支撑规则（均出自 docx 原文）：

- **会议算费**：人数限制内按金额，超员部分按超员金额，对私超员同理——人数/超员天然是"会议专属补充字段"；
- **业务组号**：业务编号分组（拜访=1、会议=4…），发包标记以业务组为单位唯一；
- **城市级别**：品种级可定制各城市线级（区别于默认线级），区域价据此取数；
- **默认价目表**：仅发版时把新增价目同步到各品种，纯开发工具，不应出现在运营视野；
- **医院拜访同类服务项复用**：把某拜访类型统一为同价（例：学术拜访三甲设 901 → 其他等级也变 901）；
- **导入导出**：解决页面上逐格改价慢的问题——批量改价的主路径。

### 2.1 隐式矩阵：对公/对私的生效规则（混乱核心）

| 药厂开关「公私合一」 | 生效表 | 金额用途 | 对私金额用途 |
|---|---|---|---|
| 公私合一 | 基础表（product_budget） | 填报 + 发包 | 不生效 |
| 公私分离**基础版** | 基础表 | 填报 + 发包 | **打绩效** |
| 公私分离**升级版**（美罗，2025-01 起） | 第二套表（product_budget_ext） | 填报 | 填报 + 绩效 |

两套表并存不是产品意图，是**不敢动老表的历史妥协**：ext 表整个为美罗新业务而生，连"是否发包金额"字段都标注"目前未使用"。这张矩阵没有任何页面显式表达，是用户体感"没逻辑"的第一来源。

---

## 3. 混乱根源诊断（六条）

| # | 根源 | 证据 | 后果 |
|---|---|---|---|
| 1 | 菜单按数据库表组织 | 功能散在【品种资料管理】【报告价目表管理】两处；命名"第二套品种价目表及比例配置" | 用户不知道哪张表和自己药厂有关 |
| 2 | 同一字段多处配置 | 金额/对私金额同时出现在基础价格、会议价格、区域价格页（Obsidian A7/A10/B7/B10 全在修重复） | 配一次不知道算不算配完 |
| 3 | 对公/对私生效规则隐式化 | §2.1 三态矩阵无页面表达；两套表的基础版/升级版语义还不一致 | 用户靠背 |
| 4 | 0/空语义不统一 | 旧系统把 0 当"未配置"，完成度误判（Obsidian A5/B5/C4 在修） | "配了 0"和"没配"分不清 |
| 5 | 复制语义混乱 | 价目表复制 / 比例表复制 / 批量复制 / 绑定（共享同步）/ 导入导出五种繁殖路径并存；原型 `copyPriceList` 只复制绑定关系不复制数据，与页面整表复制语义相反 | 分不清"拷贝一份"还是"保持同步" |
| 6 | 【原型最重】配置与消费断裂 | `PriceConfig.tsx` 本地模型（99-126 行 Leaf/PriceRow）与全局 `types.ts` 算费模型（PriceItem/PriceBook/PriceRatio）完全平行；任务创建读 `seedPriceBooks`（mockData.ts 622-667 仅 4 条 PRICE_DEFS）；对私/区域/发包标记/两级层级在 TaskExecution、taskV4、结算侧 grep 零命中 | **配了没用**——这才是"没有逻辑"的体感来源 |

---

## 4. 目标模型（本轮拍板，方案核心）

### 4.0 决策记录

| # | 决策 | 内容 |
|---|---|---|
| P1 | 菜单 | 价目管理（二级）→ **价目表配置**（唯一三级菜单）；基础/公私分离/报告三张表的列表页不再独立成菜单 |
| P2 | 列表字段 | 品种名称、药厂名称、**绑定价目表**（+ 来源标注：继承自药厂 / 品种自有副本） |
| P3 | 绑定粒度 | **药厂绑定默认价目表 → 品种自动继承；品种可解绑生成副本独立定价** |
| P4 | 价目表结构 | **一张表、四个价格区**：基础价格 / 会议价格 / 报告价格 / 区域价格；**区域价格默认关闭，需开关开启** |
| P5 | 定价例外 | 解绑生成副本（不搞字段级覆盖）；改模板只影响绑定中的品种，副本不受影响 |
| P6 | 对公/对私 | 字段常驻模型，生效与显隐由药厂开关「公私合一」显式驱动，页面明示当前模式与字段用途 |
| P7 | 配置判定 | 空=未配置，0=已配置，非 0=已配置——全链路统一（配置页、筛选、完成度、算费前置校验） |

### 4.1 信息架构与页面骨架

```text
价目管理（二级菜单）
└─ 价目表配置（唯一三级菜单）
   ├─ 列表页：品种名称 | 药厂名称 | 绑定价目表（继承自药厂 / 品种自有副本）| 操作
   │    操作：查看/编辑价目（副本或模板）· 换绑 · 解绑定制 · （批量换绑）
   └─ 价目表编辑页（一张表四个价格区 = 页签）
   ├─ 基础价格：金额、对私金额、业务组号、是否发包金额（+价目类别层级、价格来源）
   ├─ 会议价格：人数限制、超员金额、对私超员金额
   ├─ 报告价格：价目大类 / 价目小类、金额
   └─ 区域价格：一线~五线 + 对私一线~对私五线（默认关，开关开启后可配）
```

### 4.2 绑定语义（一句话规则）

> **绑定 = 共享同步；解绑副本 = 独立定制。**

- 药厂绑定默认价目表后，旗下所有品种自动继承该表价格；
- 个别品种需要特殊定价时「解绑」：系统生成该品种自有副本，此后独立维护，模板变更不影响它；
- 修改模板价目表 = 影响所有仍在绑定中的品种（影响范围在编辑页显式提示"当前 N 个品种绑定此表"）；
- 复制 / 批量复制 / 导入导出降级为辅助工具（接新品种首选换绑，实在要批量造表才用复制）。

### 4.3 四个价格区的职责边界（字段只属于一个区）

| 价格区 | 字段 | 消费端 |
|---|---|---|
| 基础价格 | 金额、对私金额、业务组号（只读展示）、是否发包金额（每业务组唯一）、价目类别层级（医院拜访 6 子类等）、价格来源（只读） | 填报取价、发包取价、绩效取对私 |
| 会议价格 | 人数限制、超员金额、对私超员金额 | 会议类填报算费 |
| 报告价格 | 价目大类、价目小类、金额 | 综服发包的报告类取价 |
| 区域价格 | 一线/新一线/二线/三线/四线/五线 + 对应 6 个对私列 | 区域模式下按城市线级取价（替代基础价格的统一金额） |

**禁止**：同一字段在多个价格区重复出现（金额只在基础价格，会议/区域页不再出现金额列——吸收 Obsidian A7/A10/B7/B10 终态）。

### 4.4 对公/对私的显式化（P6）

- 字段常驻：对私金额、对私超员金额、对私城市线级始终在模型中；
- 生效规则由药厂开关「公私合一」驱动，**编辑页顶部明示**当前模式与字段用途：
  - 公私合一 → 对私字段隐藏或只读灰显（标注"当前模式不生效"）；
  - 公私分离基础版 → 对私金额可配，标注"用于服务专员绩效"；
  - 公私分离升级版 → 金额+对私均可配，标注"金额用于填报，对私用于绩效"；
- 完成度判定：公私分离模式下，基础价格"已配置"必须同时满足金额与对私金额（吸收 B5）。

### 4.5 配套工具（收敛后）

| 工具 | 保留位置 | 说明 |
|---|---|---|
| 换绑 / 批量换绑 | 列表页操作列 | 接新品种的主路径 |
| 解绑生成副本 | 列表页操作列 | 特殊定价例外路径 |
| 医院拜访同类服务项复用 | 基础价格区顶部 | 某拜访类型统一同价 |
| 导入 / 导出 | 各价格区顶部 | 批量改价主路径（导出→表格改→导入） |
| 恢复默认 | 各价格区顶部 | 回到默认模板值 |
| 品种城市级别配置 | 从列表页跳转独立页（保持现状） | 区域价格的取数依据 |
| 默认价目表（3 套） | 移出运营导航 | 仅开发发版用 |

---

## 5. 与 Obsidian 26 条整改的关系映射

| Obsidian 条目 | 处理 | 说明 |
|---|---|---|
| A1 / B2 / C1（列表页筛选） | **吸收并升级** | 单列表页保留品种筛选（通用名/商品名/持有人），三页筛选合一 |
| A2 / B3（医院拜访复用按钮） | 吸收 | 挂到基础价格区顶部 |
| A3 / B4（医院拜访 6 子类层级） | 吸收 | 价目类别层级进基础价格区；服务目录全局唯一定义 |
| A4（对私金额不灰化） | 吸收 | 按 P6 由药厂开关显式驱动（公私分离模式下可配） |
| A5 / B5 / C4（0/空判定） | 吸收 | P7 全链路统一 |
| A6 / B6（删"只看异常项"） | 吸收 | 过滤统一为 全部/只看未配置/只看已配置 |
| A7 / B7（会议页字段收敛） | 吸收 | 会议价格区只留人数/超员/对私超员 |
| A8 / B8 / A9 / B9（会议、区域过滤项） | **简化** | 统一为 全部/只看未配置/只看已配置；**风险 1 采用方案 B**：会议区不加"只看发包金额"（可筛选但字段不可见的体验债不值得背），发包查看统一在基础价格区 |
| A10 / B10（区域页删金额、加对私线级） | 吸收 | 区域价格区 12 列，无金额列 |
| A11 / C5（删比例导出） | 吸收 | 删除无效导出 |
| B1 / C2（比例表复制、批量复制） | **降级** | 换绑为主路径后，复制类工具保留但不作为主推 |
| C3（导入报告价格） | 吸收 | 报告价格区顶部补导入 |
| A12 / D1-D3（不改项） | 沿用 | 城市级别跳转保持；默认价目表移出运营导航 |
| 三张表各自的列表页结构 | **作废** | 被单一「价目表配置」列表替代 |

---

## 6. 原型改造差距清单（评审通过后实施）

| # | 差距 | 现状（代码位置） | 改造方向 |
|---|---|---|---|
| 1 | 三个菜单入口合一 | App.tsx 665-691 三入口 price-base/price-gs/price-report | 价目管理下仅「价目表配置」一个菜单项（旧 PageId 可保留做兼容或清理） |
| 2 | 列表页改绑定模型 | PriceConfig.tsx renderList 962-1109，绑定机制为品种级单点 | 列表 = 品种×药厂×绑定表；药厂默认绑定 + 品种继承标注；操作 = 换绑/解绑副本 |
| 3 | 价目表模型四区化 | Leaf（99-111）字段全有但混在一个扁平结构；报告价格独立一套 ReportRow | 一张表四区：基础/会议/报告/区域；区域区由开关控制显隐 |
| 4 | 模型与算费打通 | PriceConfig 本地模型 vs types.ts PriceItem/PriceBook 平行；TaskDataContext.savePriceItems/savePriceRatios 为无调用死代码（1962-1994） | 统一模型：价目表编辑保存 → 写 PriceBook/rules → 任务创建（assertSamePriceBook、splitByRatio、nearestTier）消费同一份数据 |
| 5 | 发包标记消费 | isContract 仅 UI（805-824 同组切换），算费不读 | 发包取价按"业务组内发包标记项"取数 |
| 6 | 对私金额消费 | 对私字段仅存在于 PriceConfig；绩效侧（拜访管理绩效状态链）无对私 | 公私分离模式下，绩效视图取对私金额口径 |
| 7 | 区域价消费 | 价格模式开关仅 UI 置灰（1272-1276）；CITY_LEVEL_DATA 硬编码 6 城 | 区域开关开启后，拜访填报按 品种城市级别 → 线级取价 |
| 8 | 0/空判定贯通 | 仅价目页正确（259-261 isConfigured）；types.ts amount:number 表达不了"未配置" | 全局口径统一：空/undefined=未配置，0=已配置 |
| 9 | 复制语义归一 | 页面 confirmCopy 整表复制 vs context copyPriceList 只复制绑定（2035-2051） | 明确：换绑=改绑定、解绑副本=复制数据成独立表；两个动词两个按钮 |
| 10 | 比例配置去向 | renderRatioEdit（1475-1533）独立编辑页 | 比例作为价目表的组成部分（发包区）或紧邻入口，随绑定生效 |

---

## 7. 待确认项（不阻塞评审，实施前拍板）

1. **区域价格开关挂哪级**：药厂级（一个药厂统一开/关区域模式）还是品种级？——建议药厂级（与「公私合一」同层，避免品种间取价口径不一致）；
2. **公私分离 ext 表存量（美罗）过渡**：新模型下美罗药厂切到"公私分离升级版"开关 + 基础表即可覆盖，ext 表数据需一次性迁移；切换时点与灰度需业务确认；
3. **开发专用默认价目表入口去向**：移出运营导航后放哪（系统管理隐藏分组 / 仅开发环境可见）；
4. **报告价格并入一张表后的综服发包取数**：综服发包按"报告价格区"取数的链路要在算费侧显式化，确认无遗漏场景。

---

## 8. 落地节奏

| 阶段 | 内容 | 产出 |
|---|---|---|
| 本期 | ① 菜单合一 + 绑定模型（药厂默认/品种继承/解绑副本）；② 价目表四区模型统一；③ 0/空判定贯通 | 新「价目表配置」页面 + 数据模型 |
| 下期 | ① 消费端贯通（发包取价/填报区域取价/绩效对私口径）；② 区域模式算费语义；③ ext 表迁移方案 | 算费链路改造 |
| 远期 | 公私分离开关三态与价目表字段的最终收敛验证 | 全链路验收用例 |

## 9. 红线

1. 不新增审批流、复杂权限、状态机（Obsidian 方案口径）；
2. 一个字段只属于一个价格区，禁止跨区重复配置；
3. 绑定与复制语义不得混用：绑定=共享同步，副本=独立定制；
4. 默认价目表三套不进运营导航；
5. 金额全角 `￥`、状态词沿用既有业务状态，不自造新词。
