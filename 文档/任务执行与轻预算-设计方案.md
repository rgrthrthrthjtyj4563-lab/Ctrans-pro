# 任务执行与轻预算 · 设计方案

> **⚠️ 已作废（2026-08-25 下午）**：本文"费用额度 + 创建任务超额阻断"路线已被《预算计划与任务执行-设计方案.md》（V2）取代。V2 变化：预算改为"预算计划（年度+12月）不阻断"、创建任务对齐现网三张服务表、结算按服务月份发起、新增预算执行分析。本文仅作历史留痕，请勿按本文开发。

> 日期：2026-08-25
> 依据：《贝医-药合作系统用户操作手册.docx》《任务下发操作手册-药厂端.pdf》《药合作系统-菜单功能清单.xlsx》《字段采集结果.md》
> 配套阅读：《任务执行重构-问题分析.md》（讲为什么这么改）
> 本文档是**新原型的搭建依据**：按角色把操作步骤、界面字段、状态、金额规则写到可以直接照着做页面的程度。所有名词以手册原词为准。

---

## 0. 三条总原则

1. **贴手册原样优化，不过度扩展**：手册没有的功能不做（月度预算、额度分配、冻结、调钱，全部删除）；手册有的功能不丢（价目拆分、四级接力、按单结算、择期结算、完结作废）。
2. **预算做轻，与任务执行只在一个点上相交**：药厂的实际预算在 ERP 里，本系统只提供"年度费用额度 + 创建任务时一次校验"。
3. **只用手册里的词**：待确认、执行中、已结算、结算完结、对账中、待审核、发包金额、计划总金额、已结算金额、剩余可结算金额、工作量、择期结算。不发明新词。

---

## 1. 角色与分工（手册第一章原表）

| 角色 | 做什么 | 用什么端 |
|---|---|---|
| 药企（药厂） | 品种管理、授权品种给服务提供商、创建任务（发包）、审核报告（报告类）、确认结算单、结算完结、业务数据分析统计 | PC 后台 |
| 服务提供商 | 检查并接收任务、拆解任务到工作组、审核数据并给工作组考核绩效、发起结算 | 药合作后台 + APP |
| 工作组（工作组长） | 确认任务、拆解任务至服务专员、审核数据并给服务专员考核绩效 | 药合作 APP |
| 服务专员 | 标记客户、填报推广数据（先基础数据后业务数据） | 药友料 APP |

药厂内部的销售/合规分工通过账号权限实现，不设新角色。本期原型登录角色维持三项：药厂销售部门、药厂合规部门（只读）、服务提供商；工作组和专员的操作在 Web 端做桌面模拟（服务商的"拆解到工作组"弹框、任务详情里的工作量分配视图），真 APP 后续再做。

---

## 2. 业务总流程（一条链）

```
药厂                      服务提供商                工作组                 服务专员
────                      ──────────               ──────                ────────
品种管理
  ↓
品种授权（品种×区域→服务商）
  ↓
价目配置（工作量价目/比例、报告价目）
  ↓
创建任务（填总金额→按价目自动拆分→额度校验）
  ↓ [待确认] ────撤销────→ [已撤销]
  ↓ 确认
[执行中] ←────────────────
  ↓                        拆解任务包到工作组（推广金额/时区/组长）
  ↓                          ↓ 确认+创建团队
  ↓                            ↓ 分配工作量到专员 ──────→ 填报（先基础后业务）
  ↓                        审核数据+绩效             审核数据+绩效
  ↓ （报告类：药厂审核报告[待审核→通过/驳回]）
  ↓                        发起结算
[对账中]
  ↓ 药企确认结算单（可调金额+必填备注）
已结算（本次）──剩余>0──→ 择期结算（再次发起）或 结算完结（剩余作废不计统计）
[已结算]（全部结清 或 结算完结）
```

---

## 3. 轻预算：费用额度

### 3.1 定位

预算模块收敛为一个页面：**费用额度**。回答一个问题："今年这个品种在这个区域还剩多少额度可以发包。"

### 3.2 录入（居中弹框，一次填完）

| 字段 | 控件 | 必填 | 说明 |
|---|---|---|---|
| 年度 | 下拉（2026/2027…） | 是 | |
| 品种 | 下拉（品种管理里已有的品种） | 是 | 同年度+同品种+同区域不允许重复建行 |
| 区域 | 省级行政区多选，或勾选"全国" | 是 | 只用省份，不用大区；"全国"行作为兜底 |
| 年度额度 | 金额输入（￥，元） | 是 | 展示格式：￥400,000 |

### 3.3 与任务的唯一交点：创建任务时一次校验

提交新建任务时：

1. 用任务的 品种+省份 找额度行：先精确匹配省份行，没有则用该品种的"全国"行；
2. 没有额度行 → **不校验**，创建弹框顶部提示"该品种在本区域未设置年度费用额度，本次不做额度校验"（可去设置）；
3. 有额度行 → 校验：**已下发合计 + 本次计划总金额 ≤ 年度额度**；超出则阻断，提示原文：
   > 「{品种}在{区域}的年度额度剩余 ￥{剩余}，本次金额 ￥{本次} 超出 ￥{差额}。请调整金额，或由管理员调整额度。」

其中：**已下发合计 = 该品种+区域下所有未撤销任务的计划总金额之和**（撤销的任务不算；结算完结后剩余作废、不回流，所以已下发只增不减，规则极简）。

### 3.4 调整与留痕

- 调整额度：弹框改金额，**原因必填**；改小后若已下发超过新额度，允许保存但页面黄条提示"已下发金额超过当前额度，仅影响新任务创建"；
- 每次调整记一条修改记录：时间、操作人、修改前金额、修改后金额、原因；行内可查。

### 3.5 页面（一页，无下钻）

列表列：年度、品种、区域、年度额度、已下发、剩余、备注、最近修改时间、操作（调整额度、修改记录）。
顶部：新建额度（弹框）。角色：仅药厂销售部门可见可操作；服务提供商不见此页。

---

## 4. 前置配置（任务创建之前药厂要准备的三样东西）

### 4.1 品种管理

字段（两版手册取并集）：通用名、商品名、批准文号、剂型、规格、包装、单位、药品上市许可持有人（默认本药厂）、生产厂家、有效期。列表+新建/编辑弹框。未创建的品种不会出现在授权和任务的下拉里（手册规则）。

### 4.2 品种授权（服务提供商品种授权）

- 新建弹框：服务提供方（必填，下拉选已入驻启用的服务商）、品种（必填）、管理区域（必填；省份多选，提供"全国区域/清空"快捷按钮）；
- **授权规则（手册原文）：针对某品种，在同一个管理区域内，仅能对应一家服务提供方**；重复授权时阻断并提示；
- 创建任务时，"服务提供方"下拉按所选品种+区域自动过滤到被授权的服务商（这是授权数据唯一参与任务流程的地方）；
- 真实系统还有"工作组品种授权""服务专员品种授权"两层，本期不做（拆解时直接选工作组/专员，不做品种过滤）——见《问题分析》待拍板第 7 条。

### 4.3 价目配置（手册里"钱怎么算"的来源）

页面：品种价目表及比例配置（列表同真实系统：序号、通用名、商品名、批准文号、包装、规格、单位、持有人、算费方式、操作）。

**推广类——设置工作量价目表**（弹框）：

| 字段 | 说明 |
|---|---|
| 价目类别 | 如：拜访类、会议类、学术类 |
| 类别（业务项） | 如：医院拜访、科室会议、文献分享、学术推广 |
| 金额（￥） | 单价，全国统一；展示格式 ￥200/次 |
| 是否发包金额 | 勾选后该项计入发包金额拆分 |
| 是否预设值 | 勾选后创建任务时默认带出数量 |

本期不做：区域价（一线~五线城市）、对私金额、人数限制、超员金额（真实系统有，手册没提，字段预留）。

**推广类——设置工作量比例表**（弹框）：各业务类型的工作量比例，**所有比例之和必须为 100%，保存时校验**（手册规则）。比例作为费用拆分和统计的基础。

**报告类——品种报告价目表**（弹框）：报告类型（分析报告、问卷调研与分析等）× 价目（￥）；**报告比例之和必须为 100%**。

操作列：设置工作量价目表、设置工作量比例表、批量复制价目表（按品种复制，省得一个个配）；"设置城市级别"按钮预留置灰。

---

## 5. 任务执行（一个模块、一条链）

### 5.1 页面与列表

导航：任务管理 → **任务执行**（替代现在"任务执行"+"任务"两个页面）。

筛选：任务类型、任务状态、对账状态、品种、服务提供方、起止时间。

列表列（字段名与真实系统对齐）：

| 列 | 说明 |
|---|---|
| 任务编号 | 系统生成 |
| 任务类型 | 推广类 / 报告类 |
| 品种 / 服务提供方 / 执行区域（省份） | |
| 开始时间 / 结束时间 | 手册里的"时区"即起止日期 |
| 计划总金额（￥） | 按价目表拆分后的合计，可能高于最初输入金额（见 7.金额规则） |
| 已结算金额（￥） | 历次结算单最终结算金额之和 |
| 剩余可结算金额（￥） | 计划总金额 − 已结算金额 |
| 工作量分配情况 | 未拆解 / 已拆解到工作组 / 已分配到专员（进度：已完成 n / 待审核 m / 未完成 k） |
| 任务状态 / 对账状态 | 两列并行展示（真实系统即如此） |
| 操作 | 见 5.8 按角色与状态 |

### 5.2 药厂：创建任务（居中弹框，一屏两段）

**第一段 基本信息**：

| 字段 | 控件 | 必填 | 说明 |
|---|---|---|---|
| 任务类型 | 单选：推广类 / 报告类 | 是 | 决定后面的执行方式 |
| 品种 | 下拉 | 是 | 选后联动过滤服务提供方 |
| 服务提供方 | 下拉 | 是 | 只列"该品种×该区域"已授权的服务商 |
| 执行区域 | 省份多选 | 是 | 无大区 |
| 开始日期 / 结束日期 | 日期 | 是 | 手册原文"选定数据时区" |

**第二段 金额与服务项目**：

1. 填**服务总金额**（￥）——手册原文：包括推广服务和报告类服务的预算金额；
2. 点【按价目表拆分】→ 系统按该品种的价目表和比例自动生成服务项目清单：

| 业务分项 | 单价 | 数量 | 分项金额 |
|---|---|---|---|
| 医院拜访 | ￥200/次 | 120 | ￥24,000 |
| 科室会议 | ￥2,000/场 | 8 | ￥16,000 |
| … | | | |

3. 输入金额不符合标准档位时，**系统自动调整至最接近的可选金额**，行内提示"￥43,300 已调整为最近档位 ￥44,000"；
4. **支持手工修改数量和分项金额**；合计行实时更新；
5. 若拆分后合计 ≠ 输入金额，顶部说明（手册原文精神）："按价目表单价与比例拆解后合计为 ￥xx，与输入金额的差异来自档位取整，属正常逻辑。"
6. 报告类任务：服务项目来自报告价目表（分析报告费、问卷调研费等），同样自动算金额。

**提交**：执行额度校验（见 3.3）→ 通过则创建成功，任务进入**待确认**，列表自动生成该任务的对应记录。

### 5.3 药厂：撤销（仅待确认时）

待确认状态的任务，操作列有【撤销任务】，二次确认后任务变为**已撤销**，不再占用额度（已下发合计随即扣回）。

### 5.4 服务提供商：确认与拆解

- **确认**：待确认任务在服务商视角有【确认任务】。确认后任务进入**执行中**。（对应服务商 APP 的"任务核对"：每个任务包显示 品种/规格、药厂、省份、总预算/已确认金额、起止时间。）
- **拆解**（推广类）：执行中任务，服务商在任务详情【拆分任务包到工作组】：每行填 工作组、推广金额（￥）、时区（起止日期）；多次拆分累计 ≤ 任务计划总金额；界面常驻提示"请及时将任务包拆解分派到工作组"。剩余可拆金额在弹框顶部显示（对应 APP"任务明细"的"剩余预算"）。
- 报告类任务**不需要拆解**：服务商在"报告与审核"页签直接上传报告文件，进入**待审核**。

### 5.5 工作组：分配工作量（本期桌面模拟）

拆解到工作组后，工作组视角在任务详情【分配工作量到服务专员】：专员、工作量（次数/场次）、金额（￥）。分配后专员侧出现待办。真实操作在药合作 APP，本期在 Web 任务详情内以子表模拟，字段一致。

### 5.6 服务专员：填报（本期桌面模拟）

专员在药友料 APP：查看工作量（进度分：已完成/待审核/未完成）→ **先填报基础数据（标记客户）再填报业务数据**（拜访、会议、不良反应监测、医学问询、请假考勤、工作日报）→ 提交后进入待审核。本期原型：拜访管理等填报页保留，作为执行记录视图从任务详情跳转（按品种/区域/时间段过滤）。

### 5.7 审核与绩效

- 服务商审核专员/工作组提交的数据，通过后计入绩效（真实系统的"审核数据并给考核绩效""滚存打绩效"）。本期原型：沿用拜访管理页的批量审核交互，任务详情的执行视图里能看到 已完成/待审核/未完成 进度。
- **报告类：药厂审核报告**。任务详情"报告与审核"页签：报告名称、文件、审核状态（待审核/通过/驳回）、审核意见。【通过】【驳回】（驳回意见必填）；驳回后服务商可修改重新提交，再次进入待审核。

### 5.8 结算（按手册）

1. **发起**：服务商在执行中任务上【发起结算】（金额 ≤ 剩余可结算金额）→ 生成结算单，对账状态变**对账中**；
2. **确认结算单**（药厂）：结算单弹框显示 合同编号、服务品种、服务时间、制单日、服务提供方，明细行：序号、服务类型、服务项目、服务金额、**实际结算金额**、备注；底部：最终结算金额（大写+小写￥）；
3. **金额调整**：药企可修改"实际结算金额"，**必须填备注（调整原因，及与服务商达成一致的确认信息）**——手册明文；支持新增一行；
4. **确认**：点【确认】完成本次结算 → 本次金额计入已结算金额；可在【查看结算历史记录】下载对账单 PDF、下载发票、上传付款凭证；
5. **剩余金额两条路**（手册原文）：
   - **择期结算**：提示服务商可选择继续择期结算 → 剩余金额留在任务里，之后再次发起；
   - **结算完结**：药厂直接结束该任务 → 任务状态与对账状态均变为**已结算（终态）**，**剩余的结算金额作废，不再计入统计**。
6. 任务全部金额结清时，任务状态自动变为**已结算**。

### 5.9 操作列速查（角色 × 状态）

| 状态 | 药厂销售 | 服务提供商 |
|---|---|---|
| 待确认 | 撤销任务、查看 | 确认任务 |
| 执行中（推广类） | 查看 | 拆分任务包到工作组、发起结算 |
| 执行中（报告类） | 审核报告（待审核时）、查看 | 上传报告、发起结算 |
| 对账中 | 确认结算单（可调整+备注） | 查看 |
| 已结算（本次） | 查看结算历史、上传付款凭证、结算完结（剩余>0 时） | 择期再发起 |
| 已撤销 / 已结算（终态） | 查看 | 查看 |

---

## 6. 状态定义（全文只有这些，一个不加）

**任务状态**：

| 状态 | 含义 | 之后去哪 |
|---|---|---|
| 待确认 | 创建后的初始状态 | 撤销 → 已撤销；服务商确认 → 执行中 |
| 执行中 | 未完成结算完结、还有剩余可结算金额 | 发起结算 → 对账中；全部结清或结算完结 → 已结算 |
| 已结算 | 已完成结算完结，或所有结算金额都已结算 | 终态 |
| 已撤销 | 药厂撤销了待确认任务 | 终态 |

**对账状态**：未发起 → 对账中（服务商已发起，等药企确认）→ 已结算（本次确认后；若剩余>0 且未完结，可再次发起回到对账中；结算完结后为终态）。

**报告状态**（仅报告类）：待审核 → 通过 / 驳回（修改后重提 → 待审核）。

---

## 7. 金额规则汇总（全部金额字段就这几个）

| 金额 | 怎么算 | 备注 |
|---|---|---|
| 服务总金额 | 药厂创建时手填 | 只是输入起点 |
| 计划总金额 | 按价目表拆分后的分项合计 | **可能高于服务总金额**（单价档位取整+比例拆解，手册原文认定属合理逻辑）；对外展示与额度校验都用它 |
| 已结算金额 | Σ 历次结算单的最终结算金额 | 最终结算金额=调整后金额 |
| 剩余可结算金额 | 计划总金额 − 已结算金额 | 结算完结后作废不计统计（不回流额度） |
| 已下发合计（额度页） | Σ 未撤销任务的计划总金额 | 撤销即扣回；完结不扣回 |
| 剩余额度（额度页） | 年度额度 − 已下发合计 | 创建任务时校验用 |

货币规范：人民币，全角￥，千分位（￥400,000）；缩写用￥40.0万。

---

## 8. 页面与导航调整建议（对照现原型）

| 现原型 | 处置 | 新去向 |
|---|---|---|
| 预算管理：Budget / BudgetCreate / BudgetYear / BudgetMonth（4 页） | **删除** | 换成 1 页"费用额度" |
| 任务执行 task-dispatch + 任务 promotion-tasks（2 页） | **合并** | 1 页"任务执行"（本文 5.1） |
| 新建任务的"选服务商月度分配" | **删除** | 换成手册表单：品种/服务商/区域/起止/总金额+价目拆分 |
| 价目配置（disabled 占位） | **启用** | 工作量价目表、比例表、报告价目表（本文 4.3） |
| 品种管理、品种授权（disabled/缺失） | **启用** | 本文 4.1 / 4.2 |
| 拜访管理三页 | 保留 | 定位为"执行记录 + 审核"，可从任务详情跳转 |
| 服务专员结算 Settlement | 保留 | 定位为"结算统计汇总"，结算操作全部走任务执行 |
| 随检工作台、证据链复审、操作日志 | 不动 | — |
| 工作台 Dashboard | 微调 | 快捷入口与队列改跳"任务执行"（现在跳的是要被合并的 promotion-tasks） |

角色可见性：费用额度、价目配置、品种授权、创建任务 = 药厂销售专属；任务执行两角色都见（服务商只见自己的）；合规部门只读。

---

## 9. 与招标条 14-19 的对应

详见《任务执行重构-问题分析.md》第 5 节对照表。要点：额度页+创建校验满足条 14/15；手册自带的三个金额字段满足条 16；"工作量分配情况"列+待办满足条 17；条 18 的"取消释放回溯"按手册取舍（撤销不占用、完结作废不回流）已备案；条 19 由"调整必填备注 + 修改记录 + 结算历史 PDF"满足。

---

## 10. 原型改造差距清单（下一轮改代码的工作单）

1. **数据模型重写**（`types.ts`）：删 年度预算/条目/月度计划/服务商分配/调整单/11 种变动记录；新增 费用额度（含修改记录）、任务（类型/品种/服务商/区域/起止/服务总金额/计划总金额/已结算/剩余/两状态线/服务项目清单/拆解记录/工作量分配/结算单），状态枚举换成手册 7 词。
2. **页面**：新建"费用额度"页；重写"任务执行"页（列表+创建弹框+详情页签）；删除 4 个预算页与 promotion-tasks；启用价目配置、品种管理、品种授权。
3. **算钱逻辑**：价目拆分函数（就近档位调整+提示）、比例 Σ=100% 校验、计划总金额合计、剩余可结算金额、额度校验（省份优先/全国兜底/未设额度放行提示）。
4. **交互铁律**：全部弹框居中（禁 Drawer）；￥全角；区域只用省份；mock 不出现大区名。
5. **演示数据**：按新模型造 6~8 条任务覆盖全状态（待确认/执行中各阶段/对账中/已结算完结/已撤销，推广类与报告类都要有），额度数据覆盖"精确省份行/全国兜底行/未设置"三种情形。

---

## 11. 待拍板事项索引

《任务执行重构-问题分析.md》第 6 节列了 7 个待拍板问题（超额阻断、额度维度、执行中终止、报告驳回流程、择期期限、城市区域价、多层授权）。本文按"建议值"落笔：阻断、品种+省份/全国+年度、不加终止、驳回后重提、不加期限、本期不做区域价、只做服务商层授权。拍板后如有出入，只改对应小节。
