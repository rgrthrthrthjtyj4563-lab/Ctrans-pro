# 药合作系统 UI 设计基线与首批页面说明

> 版本：V1.0  
> 用途：作为纯前端高保真原型与后续页面批量生成的统一约束。  
> 事实来源：`字段采集结果.md`、`重构范围与信息架构说明.md`。未被两份文档证实的业务规则，不在 UI 中伪造为既定能力。

---

## 1. 设计目标与边界

### 1.1 目标

将旧系统按“综合业务 / 个人业务”堆叠菜单、字段密集且状态不透明的使用体验，重构为以业务闭环为中心的企业后台：

`任务配置 → 一线填报 → 审核 → 绩效/结算 → 证据链归档`

界面应优先帮助用户回答三个问题：

1. 我当前需要处理什么？
2. 这条业务数据处于什么状态、下一步由谁处理？
3. 这次操作会带来什么影响，尤其是审核、撤销、结算和删除？

### 1.2 原型边界

- 本期交付为**纯前端、可点击、可演示**的桌面端后台原型；使用模拟数据，不接真实接口。
- 默认以“平台运营”展示完整导航和操作能力；其他角色通过菜单、按钮、字段和数据范围的隐藏来体现权限差异。
- 允许演示查询、重置、切换、分页、打开详情、填写表单、触发弹窗和成功提示。
- 不模拟真实导入导出、文件上传、付款、删除、审批写库；这些操作均以确认弹窗和反馈状态表达。
- 不照搬旧系统的 53 个页面；严格采用《重构范围与信息架构说明》中的合并后模块。

---

## 2. 用户、导航与全局框架

### 2.1 默认角色与角色切换

默认角色为“平台运营”，便于完整呈现系统能力。顶部用户菜单提供仅用于原型演示的角色切换：药厂、服务提供商、工作组、服务专员、平台运营。

切换角色后，至少应体现：

- 可见的一级/二级菜单；
- 列表可见的数据范围文案；
- 页面顶部操作按钮；
- 详情页中的可编辑性与可执行操作。

权限规则以《重构范围与信息架构说明》的权限矩阵为准；“初审归属”“审核中心是否独立”等待确认项，原型保留说明标识，不做强行结论。

### 2.2 一级导航

左侧固定导航按业务阶段分组，而不是按旧系统角色分组：

1. 工作台
2. 任务管理
3. 业务填报
4. 绩效结算
5. 对账证据链
6. 主数据
7. 规则配置
8. 统计分析
9. 系统管理

二级导航使用可展开分组，名称采用信息架构文档定义的模块名称。当前菜单、面包屑和页面标题必须保持一致。

### 2.3 页面框架

```
┌──────────────── 顶部栏：面包屑 / 全局搜索 / 通知 / 企业与角色 ────────────────┐
├──────────┬─────────────────────────────────────────────────────────────┤
│ 左侧导航 │ 页面标题 + 页面说明 + 主操作                                    │
│          ├─────────────────────────────────────────────────────────────┤
│          │ 查询区（常用条件）/ 高级筛选 / 查询 / 重置                       │
│          ├─────────────────────────────────────────────────────────────┤
│          │ 统计摘要（按需）                                                 │
│          ├─────────────────────────────────────────────────────────────┤
│          │ 数据表格 / 空状态 / 加载状态                                      │
│          └─────────────────────────────────────────────────────────────┘
└──────────┴─────────────────────────────────────────────────────────────┘
```

- 内容最大宽度不设死，适配 1440px 及以上常用后台分辨率；小于 1280px 时允许表格横向滚动。
- 左侧导航可折叠；表格第一列和操作列在宽屏下可固定。
- 页面不使用多标签页堆叠；详情优先采用右侧抽屉，复杂创建与编辑采用独立页或分步弹窗。

---

## 3. 视觉令牌与交互准则

### 3.1 视觉基调

专业、安静、可信、可审计。以中性灰为底、深蓝绿色为主色，避免医疗图标化装饰和高饱和渐变。状态、金额、风险和危险操作必须优先于装饰性信息。

| 项目 | 建议值 | 使用规则 |
| --- | --- | --- |
| 主色 | `#176B5B` | 主要按钮、当前导航、关键链接 |
| 主色浅底 | `#E8F4F1` | 当前菜单、轻提示、可选中项 |
| 信息色 | `#2F6BCE` | 链接、处理中信息 |
| 成功色 | `#248A5A` | 已通过、已完成、已支付 |
| 警告色 | `#C77A16` | 待审核、即将到期、需要关注 |
| 风险色 | `#C73A3A` | 已驳回、异常、删除、不可逆操作 |
| 页面背景 | `#F5F7F8` | 内容区背景 |
| 卡片背景 | `#FFFFFF` | 查询、表格、详情卡片 |
| 主文字 | `#1F2937` | 标题、正文 |
| 次级文字 | `#667085` | 辅助说明、字段标签 |

| 项目 | 规范 |
| --- | --- |
| 字体 | 优先系统中文字体；正文 14px，表格 13px，页面标题 20px |
| 间距 | 采用 8px 基础单位；页面内边距 24px，卡片内边距 20px |
| 圆角 | 卡片 8px；输入框、按钮 6px；状态标签胶囊形 |
| 阴影 | 仅卡片和浮层使用低对比度阴影，避免大面积投影 |
| 图标 | 使用线性图标；图标必须配文字或悬浮说明，不只靠颜色传达状态 |

### 3.2 状态表达

业务状态使用统一标签，不得在不同页面替换颜色语义：

| 状态 | 标签样式 | 页面动作 |
| --- | --- | --- |
| 草稿 | 中性灰 | 编辑、提交、删除（演示） |
| 待审核 | 警告黄 | 查看、审核通过、驳回 |
| 已通过 | 成功绿 | 查看、打绩效 |
| 已驳回 | 风险红 | 查看原因、修改后重新提交 |
| 已打绩效 | 信息蓝绿 | 查看、进入结算 |
| 已结算 / 已支付 / 结算完结 | 深绿 | 仅查看与证据链关联 |
| 已撤销 / 作废 | 中性深灰 | 只读，明确显示原因与时间 |

所有详情页顶部均展示状态、最近操作时间、操作人和下一步动作提示。驳回必须展示审核意见；撤销、删除、结算完结等危险操作必须有二次确认和影响说明。

### 3.3 通用交互

- 查询区默认只展示 4–6 个最高频条件；其余条件收纳到“高级筛选”。
- 表格操作列按“查看 → 编辑 → 主要动作 → 更多”排序，危险操作放进“更多”。
- 批量操作只在选中记录后出现，并在操作前展示选中数量与受影响状态。
- 空状态要解释“为什么为空”和可执行的下一步，而不是只显示空图标。
- 金额使用千分位及 `¥`；日期使用 `YYYY-MM-DD`；时间使用 `YYYY-MM-DD HH:mm`。
- 所有反馈均使用可见的 Toast / Message；不可逆动作同时使用确认弹窗。

---

## 4. 必须先建设的通用组件与页面模板

### 4.1 通用组件

1. AppShell：顶部栏、角色切换、左侧导航、面包屑、内容容器。
2. PageHeader：页面标题、说明、数据范围提示、主操作按钮。
3. FilterBar：常用筛选、高级筛选、查询、重置、保存筛选（仅展示）。
4. DataTable：列设置、勾选、固定操作列、分页、空状态、加载骨架。
5. StatusTag：统一状态映射与文案。
6. DetailDrawer：摘要、状态轨迹、字段分组、附件区、操作区。
7. AuditDialog：审核通过/驳回；驳回意见必填，明确影响。
8. ConfirmDialog：撤销、删除、结算完结等危险动作的二次确认。
9. StepForm：任务创建、复杂填报的分步表单与草稿提示。
10. Timeline：审核、绩效、结算及证据链操作留痕。

### 4.2 页面模板

| 模板 | 适用模块 | 固定结构 |
| --- | --- | --- |
| 列表管理页 | 品种、用户、拜访、调研、报告、证据链 | 页头 + 查询区 + 摘要 + 表格 + 抽屉 |
| 配置页 | 价目、业务开关、授权 | 页头 + 范围选择 + 分组配置卡片 + 固定保存栏 |
| 任务工作台 | 推广任务、报告/问卷任务 | 页头 + 状态概览 + 筛选 + 任务表格 + 详情抽屉 |
| 分步创建页 | 新建推广任务、报告/问卷任务、复杂填报 | 步骤条 + 表单分组 + 草稿/提交操作 |
| 结算页 | 团队质量评价、服务专员结算 | 汇总卡 + 状态筛选 + 金额表格 + 明细/时间线 |

---

## 5. 首批页面范围与验收说明

首批页面的目的不是覆盖所有业务，而是验收设计系统能否承载主数据、任务、填报、审核与结算五类典型场景。

### 5.1 首批页面清单

| 优先级 | 页面 | 采用模板 | 关键演示内容 |
| --- | --- | --- | --- |
| P0 | 工作台 | 自定义概览 | 待审核、待结算、任务进度、风险/异常、快捷入口 |
| P0 | 品种管理 | 列表管理页 | 查询、品种列表、详情、资料附件、创建/编辑弹窗 |
| P0 | 企业用户 | 列表管理页 | 用户类型切换、组织归属、状态、详情与授权入口 |
| P0 | 价目配置 | 配置页 | 品种与区域选择、价目/比例/城市级别的分组配置 |
| P0 | 药厂业务开关 | 配置页 | 按药厂选择、规则分组、阈值输入、保存前确认 |
| P1 | 推广任务 | 任务工作台 + 分步创建页 | 新建、发包、工作量分配、任务状态、任务详情 |
| P1 | 拜访管理 | 列表管理页 + 详情抽屉 | 医院/商业/药房类型切换、查询、批量审核、驳回意见 |
| P1 | 会议活动 | 列表管理页 + 详情抽屉 | 会议/义诊/跟台类型切换、提交、撤销入口 |
| P1 | 调研管理 | 列表管理页 + 详情抽屉 | 5 类调研切换、差异字段分组、审核 |
| P2 | 服务专员结算 | 结算页 | 金额汇总、结算状态、工作量明细、付款凭证占位 |
| P2 | 证据链管理 | 列表管理页 + 时间线 | 对账单关联、文件列表、下载/编辑/删除确认 |

### 5.2 首批验收门槛

- 全部首批页面使用同一导航、标题、查询区、状态和操作模式。
- 每个列表页均有：模拟数据、查询、重置、分页、详情抽屉、空状态和加载状态。
- 每个关键状态均可在模拟数据中看到，并有正确的标签颜色与下一步操作。
- 推广任务、拜访审核、服务专员结算、证据链至少各有一条可点击的完整演示路径。
- 切换为“服务专员”后，只保留本人填报、任务与个人相关查询；不得保留平台审核和结算操作。
- 1440px 桌面宽度下无横向页面溢出；1280px 下表格可横向滚动，操作列仍可访问。

---

## 6. 给前端 AI 的执行约束

1. 先实现 AppShell、视觉令牌、10 个通用组件和 5 类模板；通过验收后再新增业务页面。
2. 业务页面必须基于模板和配置驱动的列/筛选/字段分组实现，禁止复制粘贴整页结构。
3. 所有模拟状态必须来源于本说明第 3.2 节和信息架构中的状态机；不得发明“处理中”“已关闭”等无定义状态。
4. 对待确认规则使用“待业务确认”提示或保守的只读展示，不得将猜测实现为业务限制。
5. 先完成首批 11 个代表页面，经产品验收后，按模块批次扩展其余页面。
6. 每批交付必须附：页面清单、可演示路径、复用组件、未覆盖项及已知限制。

---

## 7. 后续扩展顺序

在首批验收通过后，按以下批次扩展，而不是随机逐页制作：

1. **业务填报补齐**：问卷调研、随访、流向与库存、品种准入、报告管理。
2. **结算闭环补齐**：团队工作质量评价、证据链报告、报告/问卷任务。
3. **运营能力补齐**：品种授权、机构部门、工作量统计、角色权限、下载中心。
4. **后置与待确认项**：问卷统计、我的业务开关、统一审核中心，以及所有依赖管理员权限或生产数据核验的能力。

> 完成每一批后，应先统一调整组件与模板，再扩展下一批页面；禁止在单个页面中私自改变状态、颜色或操作语义。
