# 任务执行重构 · 问题分析

> **⚠️ 部分作废（2026-08-25 下午）**：本文第 5 节"预算做轻（费用额度+超额阻断）"的结论已被《预算计划与任务执行-设计方案.md》（V2）取代（预算改为不阻断的预算计划+执行分析）；第 3、4 节对旧原型的批评仍然有效，招标条 14-19 对照以 V2 第 9 节为准。

> 日期：2026-08-25
> 评判基准：《贝医-药合作系统用户操作手册.docx》《任务下发操作手册-药厂端.pdf》《药合作系统-菜单功能清单.xlsx》《字段采集结果.md》
> 结论先行：**预算应该缩成一个"费用额度"校验，任务应该合并成一条链**。具体方案见《任务执行与轻预算-设计方案.md》，本文只讲"现在哪里不对、为什么"。

---

## 1. 一句话结论

现在原型把"预算"做成了四层结构的资金管理系统（年度计划→月度计划→服务商额度分配→任务占用），把"任务"做成了两张互不相干的页面；而手册里的真实系统是：**药厂创建任务时填一个总金额，系统按价目表自动拆分，四级角色接力执行，按单结算**。两者差距不是"功能多少"的问题，是模型方向的问题。

---

## 2. 评判基准：手册里的系统长什么样

### 2.1 一条主链、四个角色

手册开头的业务流程图（原文照录）：

| 角色 | 步骤 |
|---|---|
| 药企（PC 后台） | 品种管理 → 授权品种给服务提供商 → 创建任务 → 业务数据分析统计 |
| 服务提供商（药合作 APP/后台） | 检查并接收任务 → 下派推广任务至工作组长（填写：推广金额、时区、对应工作组长）→ 审核并给工作组长考核绩效 |
| 工作组长（药合作 APP） | 确认任务 → 创建团队 → 拆解任务至服务专员 → 审核数据并给服务专员考核绩效 |
| 服务专员（药友料 APP） | 标记客户 → 填报推广数据 → 任务完成 |

手册正文对终点的描述："服务专员填报后，服务提供商审核并绩效考评，本次流程结束。"整条链没有预算审批环节。

### 2.2 "预算"在手册里的全部内容

手册里跟钱有关的流程只有一段话（PDF 手册"填写基本信息和下发金额"原文）：

> 填写服务总金额，包括推广服务和报告类服务的预算金额；系统将自动计算各项业务的分项金额和数量，并依据价目表自动匹配相应服务内容。若输入的金额不符合标准档位，系统会自动调整至最接近的可选金额。支持修改调整数量或分项金额。

即：**预算 = 创建任务时填一个总金额，系统按价目表拆成分项，就完了**。没有年度预算编制、没有月度预算、没有服务商额度分配确认、没有冻结日、没有月度间调钱。药厂的预算管理在 ERP 里做，本系统只管"这一单发包花多少钱、最后结算多少"。

### 2.3 真实系统的任务管理只有两页

《药合作系统-菜单功能清单.xlsx》里"任务管理"一级菜单下只有两个页面：

| 页面 | 管什么 | 关键列表字段 | 关键操作 |
|---|---|---|---|
| 任务下发 | 报告类/问卷类任务 | 所属品种、服务提供商、推广时段、计划总金额、已结算金额、剩余可结算金额、任务状态、对账状态 | 创建任务、审核报告（通过/驳回）、撤销任务、确认结算单、调整、查看结算历史记录、结算完结、上传付款凭证 |
| 推广任务分配管理 | 推广类任务发包与工作量分配 | 服务提供方、品种、起止时间、发包金额、执行区域、工作量分配情况、服务单价 | 新建任务、发包、给服务专员分配工作量、查看工作量、滚存打绩效、生成结算对账单 |

注意两页的字段里有同一个东西：**钱（发包金额/计划总金额）和量（工作量分配情况）**，以及**任务状态 + 对账状态两条并行的状态**。手册把它们叙述成一条链：创建（填总金额）→ 拆分 → 执行 → 审核 → 结算。区别只是报告类任务多一个"药企审核报告"环节，推广类任务由专员在移动端执行、药企无需操作。

### 2.4 状态一共就这几个（手册术语表原文）

| 术语 | 定义 |
|---|---|
| 待确认 | 任务创建后的初始状态，药企此时可撤销 |
| 执行中 | 整体任务未完成"结算完结"操作，还有剩余可结算金额 |
| 已结算 | 整体任务已完成"结算完结"操作，或所有结算金额都已结算 |
| 结算完结 | 主动结束该条任务，结束后任务状态和对账状态均为终态，剩余结算金额作废、不再计入统计 |
| 对账中 | 服务提供商已发起结算对账，等待药企确认 |
| 待审核 | 报告类文件提交后等待药企审核的状态 |

外加一个"已撤销"（药企撤销待确认任务）。**总共七个词**，覆盖了任务的一生。

---

## 3. 现在的原型 vs 手册（逐项对照）

| 维度 | 手册/真实系统 | 现在的原型 | 差距 |
|---|---|---|---|
| 预算 | 创建任务填一个总金额 | 年度预算计划→计划条目→月度预算计划→服务商月度分配→任务占用，共 4 级 6~7 个实体、4 个页面 | 方向性偏离 |
| 任务状态 | 待确认/执行中/已结算/结算完结/已撤销（5 个） | 草稿/待下发/执行中/已完成待结算/结算中/已结算/已取消（7 个） | 自造术语 |
| 对账状态 | 未发起/对账中/已结算（3 个） | 未发起/对账中/待确认/部分结算/已结算（5 个） | 自造术语 |
| 任务的入口 | 药企"创建任务"填品种/服务商/金额/时间，按价目自动拆分 | "新建任务"必须先选一条"已确认的服务商月度分配"，没有品种下单、没有价目拆分 | 创建入口被预算绑死 |
| 价目表 | 手册核心机制：按价目自动拆分分项、非标档位就近调整 | 价目页是禁用占位页；拆分函数写了但没有任何页面调用 | 核心机制缺失 |
| 金额字段 | 计划总金额/已结算金额/剩余可结算金额 | 占用额/已执行/剩余额度/释放额/偏离度/执行率……十几个自造金额 | 看不懂 |
| 移动端链路 | 服务商拆解→工作组分配→专员填报，全在 APP | 完全没有，只有 Web 端列表 | 链路断裂 |
| 结算 | 服务商发起→对账中→药企确认结算单（可调金额+备注）→择期结算/结算完结 | 结算页是独立的一套演示数据，跟任务没有代码关联 | 各干各的 |

---

## 4. 问题清单

### P1 "任务"和"任务执行"两页割裂，数据零关联

现在导航里"任务执行"组下有两个页面（`src/App.tsx`）：

- **任务执行**（`pages/TaskDispatch.tsx`）：6 条确定性数据，每条必须绑定"一条已确认的服务商月度分配"，页面描述自己写着"任务必须绑定一条已确认的服务商月度分配（任务预算占用）"；
- **任务**（`pages/PromotionTasks.tsx`）：45 条随机生成的推广明细（医生、任务类型、风险等级、申报金额），跟前者**没有任何编号关联**，"新建"按钮点了只是跳回任务执行页。

用户看不懂两页的区别是正常的——**因为区别本身没有业务依据**。它的来源是旧系统"任务下发"和"推广任务分配管理"两页并存的历史遗留（《重构范围与信息架构说明》待确认项 #14 原文："任务下发 vs 推广任务分配：是否统一为任务中心用任务类型区分，还是保留两个独立流程"）。手册给出的答案是：**这就是同一条任务链，用任务类型（推广类/报告类）区分，不需要两个模块**。

### P2 现在的"任务执行"页其实是"预算占用单"

`TaskDispatch.tsx` 的全部设计围绕额度：创建任务先选分配、校验"占用 ≤ 可用额度"、超额红字阻断；取消任务按状态分级释放额度回分配；每个动作写一条变动记录。它管的是"钱怎么在预算体系里挪"，不是手册里那个"任务"。真正的任务要素——品种、区域、起止时间、价目拆分、工作组、工作量——一个都没有（新建表单里没有品种和区域字段，因为它们来自所选分配）。

### P3 预算被建成四层资金管理系统，远超需要

现状数字（`src/types.ts`、`src/context/TaskDataContext.tsx`、`src/data/mockData.ts`）：

| 指标 | 数量 |
|---|---|
| 预算相关实体 | 6~7 个（年度计划、年度条目、月度计划、服务商分配、分配调整单、变动记录） |
| 状态机 | 预算域 4 套共 21 个状态；加任务/对账共 7 套约 36 个状态 |
| 变动记录事件类型 | 11 种 |
| 金额自造词 | 占用额、可用额度、剩余额度、释放额、偏离度、执行率、未分配额、未计划额…… |
| 数字自检函数 | 一个约 110 行、覆盖 12 条公式的校验函数 |
| 用户操作路径 | 创建→分配→确认→建任务要跨 4 个页面 |

对照手册："填一个总金额"。药厂实际用 ERP 做预算，本系统的预算只需要回答一个问题：**"今年这个品种在这个区域还剩多少额度可以发包"**。四层结构里，月度预算计划、服务商额度分配确认、月度间调钱（原设计里"≤5% 且 ≤￥50,000 自动生效"的双通道规则）、执行日前 T-5 个工作日冻结——这些机制招标文件没有要求、手册里不存在、没有任何业务方提出过需求。

### P4 状态术语自造，业务方对不上话

现原型的"草稿/待下发/已完成待结算/结算中"，业务方在手册里找不到对应说法。手册的状态词就 7 个（见 2.4），新原型应该原样使用，一个都不加。顺带说明：旧系统信息架构文档里那串"草稿→发包→已下发→填报→填报中→提交→待审核→通过→已通过→结算→结算中→完结→结算完结"十几态的长链，同样应该被手册的 7 个词取代——手册是现网真实在用的说法。

### P5 价目表机制缺失——这恰恰是手册的核心

手册里"钱怎么算"完全靠价目表驱动：

- 推广类：品种价目表及比例配置（真实系统操作列：设置工作量价目表、设置工作量比例表、批量复制价目表、设置城市级别）；工作量价目表字段包括：价目类别、类别、金额、对私金额、人数限制、超员金额、区域价（一线~五线城市及对私）、是否发包金额、是否预设值；
- 报告类：品种报告价目表（报告比例合计必须 100%）；
- 创建任务时：填总金额 → 系统按单价和比例自动算出各业务的数量和分项金额 → 非标金额自动调到最近档位 → 允许手工改数量和分项金额 → **下发显示的金额总和可能高于输入金额**（手册原文解释：按单价+比例拆解汇总所致，属合理逻辑）。

现原型里：价目配置页是禁用占位页（`App.tsx` 中 disabled），`constants.ts` 里写了一个 4 档静态价目和拆分函数，但**没有任何页面调用它**。等于把手册最核心的算钱机制整个跳过了，却把力气花在预算状态机上。

### P6 移动端链路（手册 3.3~3.5 的主体）完全缺失

手册从服务商确认任务开始的每一步都在 APP 上：服务商 APP"任务核对"（每个任务包显示：品种、药厂、省份、总预算/已确认金额、起止时间）→"任务明细"（时间范围、预算费用、省份、剩余预算；按钮：拆分任务包到工作组长、查看工作量）→ 工作组 APP 分配工作量到专员 → 专员在"药友料"APP 看工作量（进度分：已完成/待审核/未完成；任务卡片带计划金额）先填基础数据再填业务数据。现原型没有一屏体现这条链，任务详情里"执行与证据"页签是写死的演示文案。

### P7 结算页各干各的，手册的结算规则没实现

手册结算规则：服务商发起 → 对账状态变"对账中" → 药企确认结算单（界面上有：服务类型、服务项目、服务金额、实际结算金额、备注；支持调整金额、**调整必须备注原因**）→ 确认后可下载结算对账单 PDF、下载发票、上传付款凭证 → **有剩余金额时提醒服务商可择期结算，药厂也可直接结束任务；结束后剩余金额作废不计统计**。

现原型：`pages/Settlement.tsx` 是一套独立的结算演示数据（待审核/已通过/已驳回/已结算），与任务列表的"对账状态"字段没有代码关联；择期结算、结算完结、作废规则都没有。

### P8 满篇内部行话，业务方读不懂

页面列头和设计文档里大量出现："口径"（列头还带注释符号解释是哪种算法）、"额度账本/实绩账本"、"T-5 工作日滚动冻结"、"调拨双路径"、"追加式流水"、"偏离度"……这些词手册里一个都没有。业务方（药厂销售、服务商）看页面时的反应就是"看不懂"，这正是本次反馈的直接来源。**新设计全部使用手册原词**：发包金额、计划总金额、已结算金额、剩余可结算金额、工作量、待确认、执行中、对账中、择期结算、结算完结。

---

## 5. 预算为什么可以做轻：对照招标条 14-19

招标文件（CG20240014）第 14-19 条是预算管控的合规基线，逐条对照：

| 条目 | 要求 | 轻预算怎么满足 | 备注 |
|---|---|---|---|
| 14 | 具备预算管理能力 | "费用额度"页面：药厂按 年度+品种+区域 录入额度，显示总额/已下发/剩余 | 一个页面解决 |
| 15 | 预算总额管控，超额阻断/预警 | 创建任务时校验：同品种同区域的未撤销任务计划总金额 + 本次金额 ≤ 额度，超出直接阻断并提示 | 唯一与任务的交点，一次校验 |
| 16 | 预算与实际执行对照 | 任务列表自带：计划总金额、已结算金额、剩余可结算金额；额度页显示已下发合计 | 用手册字段直接满足，不新造算法 |
| 17 | 未分配预警 | 手册语境：任务未拆解到工作组、工作量未分到专员 → 列表"工作量分配情况"列 + 首页待办提醒 | 对齐手册而非新造"月度未分配"概念 |
| 18 | 取消/终止后额度释放回溯 | **按手册规则执行，与招标字面有差异，明示如下**：待确认撤销 → 不占额度（等同释放）；结算完结 → 剩余金额作废不计统计（不回流，手册明文） | 用户已拍板"按手册原样优化"；差异点在此备案 |
| 19 | 调整需审批、全链留痕 | 结算金额调整必须填备注原因；额度修改保留修改记录（谁/何时/改前改后/原因）；结算历史可下载 PDF | 满足 |

结论：除了条 18 的"回流"细节按手册取舍（备案），其余各条用一个额度页 + 任务创建时一次校验即可全部满足。**四层预算结构不是合规要求，是自加的复杂度。**

---

## 6. 需要你拍板的问题

写设计方案前，以下几个点手册没有明说，需要拍板（文档二里已按"建议"先写着，改起来快）：

1. **超额处理**：创建任务超出额度时，直接阻断（招标条 15 的字面要求），还是允许填理由后继续？→ 建议：阻断。
2. **额度维度**：按"品种+区域（省/全国）+年度"录额度行；任务匹配时先找省份行、找不到用"全国"行兜底。是否可以？→ 建议：可以。
3. **执行中任务能否终止**：手册只有"待确认可撤销"和"结算完结"两种结束方式，没有"终止执行中的任务"。→ 建议：不加，保持手册原样（真要中止就等结算时按实际发生金额结）。
4. **报告审核驳回后的流程**：手册只写了驳回，没写后续。→ 建议：驳回后服务商修改重新提交，再次进入"待审核"。
5. **择期结算期限**：手册无期限规定。→ 建议：不加期限，剩余金额一直挂到结算完结。
6. **价目表的城市区域价（一线~五线）**：真实系统有，手册一句没提。→ 建议：本期只做全国统一单价，城市级别字段预留不实现。
7. **多层品种授权**：真实系统有三层（服务商/工作组/专员各一层品种授权页），手册只讲服务商这一层。→ 建议：本期只做"服务提供商品种授权"（任务下发的前置条件），拆解时直接选工作组/专员、不做品种级过滤。

---

## 7. 附：本分析引用的证据位置

| 证据 | 位置 |
|---|---|
| 手册正文与流程图 | `重构/贝医-药合作系统用户操作手册.docx`（角色定义表、3.2~3.5 章、内嵌流程图与界面截图） |
| 下发/结算/术语 | `重构/药合作操作手册-药企.pdf`（价目配置、下发、待确认/撤销、结算流程、术语表） |
| 真实系统菜单 | `药合作系统-菜单功能清单.xlsx`（任务管理两页、品种价目、报告价目、授权、对账单管理） |
| 两页字段与操作明细 | `重构/字段采集结果.md` 2812-2867 行（价目）、3285-3359 行（任务下发）、3361-3423 行（推广任务分配管理） |
| 现原型实现 | `重构/业务分析与设计准备/src/`：`App.tsx`（导航）、`pages/TaskDispatch.tsx`、`pages/PromotionTasks.tsx`、`pages/Budget*.tsx`、`pages/Settlement.tsx`、`types.ts`、`context/TaskDataContext.tsx`、`data/mockData.ts`、`constants.ts` |
