# 预算与任务执行优化设计需求文档

> 存档说明：本文为 2026-08-25 用户定稿的需求文档原文，是《预算计划与任务执行-设计方案.md》（V2）的直接输入。
> 同日上午的《任务执行与轻预算-设计方案.md》（费用额度+超额阻断路线）自本文定稿起作废。

## 一、现状问题

- **费用额度**页以“年度额度 / 已下发 / 剩余”为核心，并在创建任务时阻断超额；这把预算变成了业务执行的门槛。
- 当前不存在月度预算页面，也没有“预算 vs 已结算实际”的偏离分析，年度计划无法服务后续决策。
- **任务执行**页已经具备任务、执行、结算的核心链路，但表格字段过多，用户难以一眼判断“这条任务下一步该做什么”。
- **服务专员结算统计**与任务详情中的结算操作并存，且目前数据来源独立；同一笔钱可能在两处出现不同状态或金额。
- 当前“计划总金额、已结算金额、剩余可结算金额”是正确的任务金额语言；“额度占用、剩余额度、释放额度”不应继续出现在业务执行链路中。

## 二、目标信息架构

```text
预算计划
年度预算 → 月度预算 ───────────────┐
                                   ├→ 预算执行分析
任务执行
药厂发包 → 服务商确认 → 执行 → 结算 ┘
```

导航调整：

- 将“费用额度”改为 **预算计划**。
- 保留 **任务执行**，作为药厂发包、服务商执行与结算的唯一业务入口。
- 将“服务专员结算”改为 **结算明细**，仅做统计与下钻，不再承载审核、确认或完结操作。
- 新增 **预算执行分析**，作为预算与实际关联的唯一入口。

## 三、页面需求

### 1. 预算计划

预算只表达企业计划，不限制任务创建、执行或结算。

- 年度预算：按年度、品种、区域、服务类型维护预算总额。
- 月度预算：年度预算下展开 12 个月，支持逐月编辑。
- 系统展示“月度预算合计 − 年度预算”的差异，仅提示，不阻断保存。
- 编辑月度预算后，以当前最新金额为准计算后续偏离；不提供初始版本对比或预算占用流水。
- 删除或替换全部“已下发、剩余、超额、调整额度、修改记录”业务表达。
- 页面主视图采用“年度总览 + 12 月预算表”：
  - 顶部：年度预算、月度预算合计、两者差异。
  - 主表：品种、区域、服务类型及 1—12 月预算。
  - 支持按品种、区域、服务类型筛选。

### 2. 任务执行

任务是药厂对服务商的发包承诺，与预算计划独立。

- 创建任务时保留：任务类型、品种、服务商、区域、起止时间、服务项目、发包计划金额。
- 删除创建任务时的年度额度匹配、金额阻断、无额度提示、额度占用与撤销回流逻辑。
- 保留任务内的计划总金额、已结算金额、剩余可结算金额；它们只解释该任务的合同与结算进度。
- 列表默认展示：任务编号、品种/服务商、执行周期、计划金额、已结算金额、执行进度、当前状态、待办动作。
- 将“任务状态”和“对账状态”收敛为用户可理解的主状态与辅助状态：
  - 主状态：待服务商确认、执行中、待药厂处理、已结算、已撤销。
  - 辅助状态仅在发生结算或报告审核时展示，如“对账中”“报告待审核”。
- 操作列只显示当前角色的下一步动作，其他动作进入详情页，避免一行出现大量并列按钮。
- 任务详情采用固定步骤条：发包 → 确认 → 执行分配 → 执行完成 → 结算；用户可直接看到当前卡点和责任人。

### 3. 结算明细

结算数据必须以任务中的已确认结算单为唯一来源。

- 结算明细页不再单独审核、驳回或完结结算单。
- 页面展示结算单号、关联任务、品种、服务商、执行归属月份、确认结算金额、确认日期。
- 点击任意结算单跳转任务详情的结算页签；所有修改、确认与付款凭证操作只在任务中完成。
- 保留服务专员维度的统计，但只能由任务执行明细汇总生成。

### 4. 预算执行分析

这是药厂决策页，不是任务下发页面。

- 筛选维度：年度/月度、品种、区域、服务类型、服务商。
- 核心指标：
  - 月度预算
  - 已结算实际
  - 偏离额 = 已结算实际 − 月度预算
  - 偏离率 = 偏离额 ÷ 月度预算
  - 未结算任务计划金额，作为未来风险提示，不计入实际和偏离
- 主图采用“预算与实际并列柱状图”，月份为横轴；偏离额以颜色和数值突出。
- 明细表按“品种 × 区域 × 服务类型 × 月份”展示预算、实际、偏离、未结算任务金额。
- 点击实际金额下钻到结算单；点击未结算任务金额下钻到任务列表。
- 月度预算为 0 时，偏离率显示“无预算”，不显示无意义的百分比。

## 四、跨月任务与实际归属

- 月度实际只取**已确认结算金额**。
- 跨月任务的已确认结算金额，按已审核执行量或服务明细的金额比例归属到各执行月份。
- 药厂确认结算单时，系统自动生成“月度归集”明细；归集依据为任务内已审核的服务日期、数量和金额。
- 若任务缺少可按月归属的执行明细，系统提示补齐归属信息后再完成结算确认，避免将整笔跨月费用粗暴归入结算月。

> 2026-08-25 拍板修正：本章按“**按服务月份发起结算**”执行——服务商发起结算时先选定一个服务月份、勾选该月已审核任务量，一张结算单只对应一个月份，确认后整单归该月；不做按金额比例的跨月拆分。任务的服务地区为**单选一个省**，实际金额按任务服务地区直接归属。

## 五、验收标准

- 月度或年度预算不足时，药厂仍可创建任务、服务商仍可执行、药厂仍可确认结算。
- 调整预算后，预算执行分析立即基于最新预算重算偏离。
- 已结算金额只来自任务内已确认结算单；结算明细页与任务详情金额完全一致。
- 跨月任务结算后，各月实际金额之和等于该结算单确认金额。
- 预算分析可从任一偏离数字追溯到任务和结算单。
- 药厂用户可在任务列表一眼看到下一步责任人；服务商不能查看或编辑预算计划。
