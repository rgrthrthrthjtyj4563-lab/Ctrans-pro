# 预算计划与任务执行 · 设计方案（V2）

> 日期：2026-08-25（下午定稿）
> **取代**：《任务执行与轻预算-设计方案.md》（2026-08-25 上午）——其"费用额度 + 创建任务超额阻断"路线整体作废
> 依据：《预算与任务执行优化设计需求文档》（同日定稿，见 `重构/预算与任务执行优化设计需求文档.md`）、《任务下发操作手册-药企.docx》（实际路径 `beiyi/研发相关资料/文档/`，与《药合作操作手册-药企.pdf》同源）、《贝医-药合作系统用户操作手册.docx》、《字段采集结果.md》、现网"任务下发"页面实拍（创建任务三张服务表、任务下发列表）
> 本文档是新原型的搭建依据，所有字段、状态、金额规则写到可直接照做页面的程度。

---

## 0. 一句话结论

**预算只表达企业计划，不做执行门槛；任务按现网模型创建（一个任务混合推广+报告服务）；结算按服务月份发起（一张结算单只对应一个月份）；预算执行分析用"已结算实际"对比"月度预算"看偏离。**

---

## 1. 与 V1（上午方案）的差异

| 维度 | V1（已作废） | V2（本文） |
|---|---|---|
| 预算 | 费用额度一页；创建任务时超额阻断 | 预算计划页（年度+12个月）；与任务零交点，不阻断 |
| 创建任务 | 任务类型单选（推广/报告）+ 填总额自动拆分 | 对齐现网：三张服务表同时可填（推广+报告混合），逐项填推广金额、系统换算数量 |
| 服务地区 | 省份多选 | **单选一个省** |
| 结算 | 对整任务发起，一张单可跨月 | **按服务月份发起**：先选月份、勾选该月已审核任务量；一张单=一个月份 |
| 统计 | 额度页"已下发/剩余" | 预算执行分析页（偏离额/偏离率），数据全部来自已确认结算单 |
| 状态 | 手册 7 词直接展示 | 数据层仍是手册 7 词，**展示层**映射为主状态+辅助状态 |
| 页面 | 费用额度 / 任务执行 / 服务专员结算统计 | 预算计划 / 预算执行分析 / 任务执行 / 结算明细 |

---

## 2. 总原则

1. **预算不设门槛**：预算不足不阻断任务创建、执行、结算（需求文档验收标准第一条）。
2. **一个金额真相**：所有已结算数字只来自任务内已确认的结算单；结算明细、预算执行分析都从它汇总，不设第二套数据。
3. **术语边界**：业务链路只用手册词；"年度预算、月度预算、已结算实际、偏离额、偏离率、未结算任务计划金额"七个词**只准出现在预算计划与预算执行分析两页**。新词解释：
   - **偏离额** = 已结算实际 − 月度预算（正数为花超，负数为节省）；
   - **偏离率** = 偏离额 ÷ 月度预算（月度预算为 0 时显示"无预算"，不算百分比）；
   - **未结算任务计划金额** = 已创建但尚未结清的任务的计划总金额，提示未来支出风险，不计入实际和偏离；
   - **任务量** = 专员完成并审核通过的工作量执行记录（带服务月份、数量、金额）；
   - **服务月份** = 任务量实际发生的月份（结算按它归属）。
4. **状态两层**：数据层手册 7 词一个不改；展示层映射成业务方一眼能懂的主状态+辅助状态（见 5.5）。

---

## 3. 信息架构与导航

```
预算计划
年度预算 → 月度预算 ───────────────┐
                                   ├→ 预算执行分析
任务执行
药厂发包 → 服务商确认 → 执行 → 结算 ┘

结算明细（只读汇总与下钻，无任何操作）
```

| 现原型 | 处置 | 新去向 |
|---|---|---|
| 费用额度（FeeQuota.tsx） | 重写 | 预算计划（BudgetPlan.tsx） |
| 任务执行（TaskExecution.tsx） | 改造 | 任务执行（列收敛+创建表单对齐现网+按月结算+步骤条） |
| 服务专员结算统计（Settlement.tsx） | 重写 | 结算明细（数据源=任务结算单，删独立 mock 与审核/完结按钮） |
| 统计分析（disabled 占位） | 启用 | 预算执行分析（BudgetAnalysis.tsx） |
| 价目配置 / 品种管理 / 品种授权 | 保留 | 价目类别按 5.1 调整 |

角色可见性：**预算计划、预算执行分析 = 药厂销售专属**（合规只读、服务商不可见）；任务执行两角色共用（服务商只见自己的任务）；结算明细药厂与服务商可见（服务商只见自己）。

---

## 4. 预算计划页

**定位**：只回答"今年这个品种在这个区域这类服务，计划花多少钱、每月怎么排"；不管执行。

### 4.1 预算行

一行 = **年度 + 品种 + 区域 + 服务类型**。

| 字段 | 控件 | 说明 |
|---|---|---|
| 年度 | 下拉 | 同年度+品种+区域+服务类型不允许重复建行 |
| 品种 | 下拉 | 品种管理里已有的品种 |
| 区域 | 单个省级行政区 或 "全国" | 只用省份，不用大区 |
| 服务类型 | 下拉 | =价目类别：市场推广服务 / 分析报告服务 / 问卷调研与分析服务 |
| 年度预算 | 金额输入（￥） | 展示格式 ￥400,000 |
| 1—12 月预算 | 12 个金额输入 | 新建时默认 = 年度预算 ÷ 12，逐月可改 |

### 4.2 页面

- 顶部三卡：**年度预算合计 / 月度预算合计 / 差异**（差异=月度合计−年度预算，正负着色，仅提示，不阻断保存）。
- 主表：品种、区域、服务类型、1—12 月、月度合计、年度预算、差异。
- 筛选：年度、品种、区域、服务类型。
- 新建/编辑：居中弹框一次录入；行内"编辑月度预算"弹框逐月改。
- **明确不做**：已下发、剩余、超额阻断、调整额度原因、修改记录、版本对比、预算占用记录。

---

## 5. 任务执行页

### 5.1 创建任务（对齐现网表单，居中弹框）

**基本信息**：

| 字段 | 控件 | 说明 |
|---|---|---|
| 任务名称 | 只读展示，自动生成 | = 品种商品名_规格_持有人（如：测试品种商品名_100片装_测试药厂企业） |
| 品种 | 下拉 | 选后联动生成任务名称、过滤服务商 |
| 服务地区 | 下拉，**单选一个省** | 区域规范：仅省份，无大区 |
| 服务提供商 | 下拉 | 只列"该品种×该省"已授权的服务商 |
| 推广时间 | 起止日期 | 现网叫法即"推广时间" |
| 记住选项 | 复选框 | 勾选后记住本次 品种/服务地区/服务提供商/推广时间，下次创建自动带出 |

**三张服务表（同一任务可同时填写，即推广+报告混合）**：

| 分组 | 服务表 | 列 | 金额逻辑 |
|---|---|---|---|
| 推广类 | 市场推广服务 | 服务项目、单价、推广金额（输入）、系统换算数量（自动） | 数量 = 推广金额 ÷ 单价（支持就近档位取整提示） |
| 报告类 | 调研与报告一体化服务 · 分析报告服务 | 服务项目、单价、推广金额（输入）、系统换算数量（自动） | 同上 |
| 报告类 | 调研与报告一体化服务 · 问卷调研与分析服务 | 问卷样本量（单价/样本量/小计）+ 分析总结（单价/数量/小计）+ 合计 | 双价目行分别填量，小计自动 |

- 每张表有各自合计行；表尾总计一行：**服务总金额（计划总金额）**= 三张表合计。
- 不填的表留空即可（只填市场推广服务 = 纯推广任务；只填报告表 = 纯报告任务）。
- **提交时不做任何预算/额度校验**；创建成功进入**待确认**。
- 价目来源：品种价目表及比例配置，价目类别=上述三类（市场推广服务、分析报告服务、问卷调研与分析服务），比例 Σ=100% 校验保留。

### 5.2 执行链（与手册一致）

待确认（药厂可撤销）→ 服务商确认 → 执行中 → 拆解任务包到工作组（推广金额/时区/组长）→ 分配工作量到专员 → 专员填报 → 审核。

- **每条工作量记录带"服务月份"**（执行发生的月份，如 2026-07）；审核通过（进度=已完成）的任务量才可被选入结算。
- 任务含报告服务时：服务商上传报告 → **待审核** → 药厂通过/驳回（驳回意见必填，修改后重提）。
- 撤销：仅待确认可撤销；已撤销任务不计入任何统计。

### 5.3 按服务月份结算

1. **发起（服务商）**：任务详情【发起结算】→ **先选服务月份**（下拉=该任务存在已审核任务量的月份）→ 勾选该月已审核任务量 → 金额=所选任务量金额合计，须 ≤ 剩余可结算金额 → 生成结算单，对账状态=**对账中**。一张结算单只对应一个月份。
2. **确认（药厂）**：结算单居中弹框，明细行：服务类型、服务项目、服务金额、**实际结算金额**、备注；可调整金额，**调整必须填备注**（调整原因及与服务商达成一致的信息）；确认后金额计入已结算金额，**整单归入选定的服务月份**。
3. **择期结算**：剩余金额留在任务里，下个月再按月发起。
4. **结算完结**：药厂主动结束任务，任务状态与对账状态均变**已结算（终态）**，剩余金额作废不计统计（手册明文）；剩余可结算金额归零。
5. 结算历史可查看/下载对账单 PDF、上传付款凭证。

### 5.4 列表（列收敛）

| 列 | 说明 |
|---|---|
| 任务编号 | 系统生成 |
| 品种 / 服务提供商 | 一列两行 |
| 服务地区 | 单省 |
| 推广时段 | 起止日期 |
| 计划总金额（￥） | 三张服务表合计 |
| 已结算金额（￥） | Σ 历次结算单最终结算金额 |
| 执行进度 | 工作量：已完成 n / 待审核 m / 未完成 k（纯报告任务显示"报告 n 待审核"） |
| 当前状态 | 主状态 + 辅助状态徽标（见 5.5） |
| 待办动作 | 仅当前角色的下一步动作，单按钮（其余操作进详情） |

### 5.5 状态展示映射（数据层 → 展示层）

| 数据层组合 | 主状态 | 辅助状态 | 下一步责任人 |
|---|---|---|---|
| 待确认 | 待服务商确认 | — | 服务商（药厂可撤销） |
| 执行中（未拆解） | 执行中 | — | 服务商 |
| 执行中（已拆解/已分配） | 执行中 | — | 按分配链 |
| 执行中 + 对账中 | **待药厂处理** | 对账中 | 药厂 |
| 执行中 + 报告待审核 | 执行中 | 报告待审核 | 药厂 |
| 已结算（终态） | 已结算 | — | — |
| 已撤销 | 已撤销 | — | — |

数据层枚举不变：任务状态=待确认/执行中/已结算/已撤销；对账状态=未发起/对账中/已结算；报告状态=待审核/通过/驳回。

### 5.6 任务详情（居中弹框）

- 顶部**固定步骤条**：发包 → 确认 → 执行分配 → 执行完成 → 结算；当前卡点高亮并标注责任人。
- 页签：服务项目清单 / 执行与工作量 / 报告与审核 / 结算记录。
- 所有操作（拆解、分配、发起结算、确认结算单、完结、凭证）都在详情内完成。

---

## 6. 结算明细页（原"服务专员结算统计"）

- **数据源唯一**：全部任务的结算单（含未确认的，按状态展示）；无独立 mock、无独立状态机。
- **无任何操作**：不审核、不驳回、不完结（这些都在任务执行里）。
- 列：结算单号、关联任务（点击跳任务详情-结算记录页签）、品种、服务提供商、执行归属月份、结算状态（对账中/已结算）、确认结算金额、确认日期、付款凭证。
- 服务专员维度统计：由工作量分配 + 结算明细汇总生成（专员、工作组、结算周期、金额）。

---

## 7. 预算执行分析页（药厂决策页）

- 筛选：年度、月份、品种、区域、服务类型、服务提供商。
- 指标卡（按筛选范围汇总）：月度预算、已结算实际、偏离额、偏离率、未结算任务计划金额。
- 主图：**预算与实际并列柱状图**，横轴=月份，两根柱=月度预算/已结算实际，偏离额以颜色和数值标注。
- 明细表：**品种 × 区域 × 服务类型 × 月份**，列=月度预算、已结算实际、偏离额、偏离率、未结算任务计划金额。
- 下钻：点"已结算实际"→ 结算单列表弹框；点"未结算任务计划金额"→ 任务列表弹框。
- 计算口径：
  - 已结算实际 = Σ 已确认结算单的行实际结算金额，归属维度 = 结算单.执行归属月份 × 任务.服务地区 × 任务.品种 × 明细行.服务类型；
  - 偏离率：月度预算为 0 时显示"**无预算**"；
  - 未结算任务计划金额（一期口径，备案）：未撤销且未结清任务的计划总金额，**按推广开始月全额计入该月**；后续如需按月分摊再升级；
  - 调整预算后立即以最新预算重算偏离。

---

## 8. 金额字段总表

| 词 | 允许出现的位置 | 怎么算 |
|---|---|---|
| 服务总金额（计划总金额） | 任务链路 | 三张服务表合计 |
| 已结算金额 | 任务链路、结算明细、分析 | Σ 已确认结算单最终结算金额 |
| 剩余可结算金额 | 任务链路 | 计划总金额 − 已结算金额（结算完结后=0） |
| 发包金额、工作量 | 任务链路（拆解/分配） | 沿用手册词 |
| 年度预算 / 月度预算 | 预算计划、预算执行分析 | 药厂录入 |
| 已结算实际 / 偏离额 / 偏离率 / 未结算任务计划金额 | 仅预算执行分析 | 见第 7 节 |
| ~~年度额度 / 已下发 / 剩余额度 / 占用 / 释放 / 超额~~ | **全部作废** | — |

货币：人民币全角￥+千分位（￥400,000），缩写￥40.0万。

---

## 9. 与招标条 14-19 对照（按"预警"通道重述）

| 条目 | 要求 | V2 怎么满足 | 备注 |
|---|---|---|---|
| 14 | 预算管理能力 | 预算计划页（年度+月度预算维护） | |
| 15 | 预算总额管控，超额阻断/预警 | 按**预警**通道：分析页偏离提示；任务创建不做预算阻断 | 业务已拍板"预算不做执行门槛"；与"阻断"字面的差异在此备案 |
| 16 | 预算与实际执行对照 | 预算执行分析页（月度预算 vs 已结算实际） | 比 V1 更直接满足 |
| 17 | 未分配预警 | 任务列表"执行进度"列（未拆解/已拆解/已分配+完成情况）+ 待办动作 | |
| 18 | 取消/终止后释放回溯 | 无额度概念，撤销不产生占用；结算完结剩余作废不回流（手册明文） | 备案 |
| 19 | 调整需审批、全链留痕 | 结算调整备注必填 + 结算历史 PDF + 操作日志；预算修改记录不做（预算为纯计划，业务拍板） | 备案 |

---

## 10. 原型改造工作单

| 文件 | 动作 |
|---|---|
| `src/types.ts` | 删 FeeQuota 族；Task 改单省/删 taskKind/删 serviceAmount；ServiceItem 挂价目类别与分组；WorkloadAssign+服务月份；SettlementBill+执行归属月份；新增 BudgetPlan |
| `src/context/TaskDataContext.tsx` | 删额度校验与额度操作；新增预算计划 CRUD、startSettlementByMonth、分析聚合函数 |
| `src/data/mockData.ts` | 三价目类别重造价目；预算计划 ≥4 行；任务 6-8 条全状态+跨月；任务量带服务月份；按月结算单样例；删 settlementRecords |
| `src/pages/BudgetPlan.tsx` | 新建（替代 FeeQuota.tsx，删除旧文件） |
| `src/pages/TaskExecution.tsx` | 创建弹框重做（三张服务表）；列收敛；状态映射；按月结算；详情步骤条 |
| `src/pages/Settlement.tsx` | 重写为结算明细（任务结算单汇总，只读） |
| `src/pages/BudgetAnalysis.tsx` | 新建 |
| `src/App.tsx` | 导航：预算计划/预算执行分析/任务执行/结算明细；角色可见性 |

交互铁律：全部居中 Modal（禁 Drawer）；￥全角；区域仅省份；mock 不出现大区名；金额展示走 formatCNY。
