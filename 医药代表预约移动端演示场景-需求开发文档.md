# 医药代表预约移动端演示场景

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档类型 | 演示场景需求开发文档 |
| 使用范围 | 贝医药合作系统演示版 |
| 目标用户 | 产品、设计、前端、演示人员 |
| 建议入口 | 现有“演示账号”菜单下新增“演示场景” |
| 实现方式 | 前端本地状态模拟，不接真实小程序、短信、后端接口 |

## 2. 背景与目标

### 2.1 背景

当前系统以桌面端后台为主，需要补充一个移动端演示场景，用来展示医药代表进入医院前完成预约，并经过预约小程序转发许可确认后，将结果同步回医药代表移动端的完整过程。

### 2.2 演示目标

让演示人员在 1 分钟内完成以下闭环：

> 医药代表发起预约 → 发送预约小程序转发许可 → 医院侧同意 → 系统同步 → 医药代表移动端看到已确认预约

### 2.3 非目标

本次只服务于产品演示，不建设以下能力：

- 不接入真实微信小程序或其他第三方平台；
- 不发送真实消息、短信或微信通知；
- 不建设真实医院账号、授权回调和接口鉴权；
- 不实现复杂的预约排班、冲突校验和审批规则；
- 不新增正式业务角色，不改动现有权限矩阵。

## 3. 入口与切换方式

### 3.1 入口位置

沿用左侧栏底部现有“演示账号”菜单，不新增一级菜单，避免打断现有后台导航。

在当前“角色切换”区域下增加分隔线和“演示场景”区域：

```text
演示账号
├─ 角色切换
│  ├─ 药厂合规部门
│  ├─ 药厂销售部门
│  └─ 服务提供商
├─ 演示场景
│  └─ 医药代表移动端
└─ 退出登录
```

“医药代表移动端”是设备/场景切换，不是登录角色。切换后不改变当前角色权限，仅改变当前演示视图。

### 3.2 切换交互

1. 点击左侧底部“演示账号”，展开现有菜单。
2. 点击“医药代表移动端”。
3. 主内容区域切换为移动端手机外框视图。
4. 左侧导航和顶部后台工具栏暂时隐藏，避免桌面后台元素干扰演示。
5. 页面顶部保留轻量的“演示场景：医药代表移动端”提示，并提供“返回后台”按钮。
6. 点击“返回后台”后恢复进入移动端前的页面和滚动位置。

### 3.3 切换时的顺滑表现

- 不使用整页白屏或加载动画；
- 手机外框和内容使用 180～240ms 的淡入/位移动画；
- 切换期间不改变当前角色；
- 如果预约流程已经进行到一半，返回移动端时保留当前数据和状态；
- 返回后台后不弹出重复提示，只在必要时显示一次轻提示；
- 支持键盘 Escape 返回后台，按钮具备明确焦点样式。

## 4. 页面结构

移动端使用桌面页面中的手机设备框展示，建议宽度约 390px，高度约 760px。内容模拟手机页面，不要求真正适配所有移动设备。

### 4.1 移动端首页

页面标题：**我的预约**

内容包括：

- 顶部问候：医药代表姓名；
- 当前日期；
- 主按钮：“新建医院预约”；
- 预约卡片列表；
- 底部导航：预约、消息、我的。

首屏预置一条示例数据，便于演示人员直接进入详情；同时提供“新建医院预约”按钮，支持从头演示。

### 4.2 新建预约页

字段控制在演示所需范围内：

| 字段 | 类型 | 示例 | 是否必填 |
|---|---|---|---|
| 医院 | 选择器 | 华安市人民医院 | 是 |
| 科室 | 选择器 | 心内科 | 是 |
| 预约日期 | 日期选择 | 2026-08-28 | 是 |
| 预计到院时间 | 时间选择 | 09:30 | 是 |
| 拜访事项 | 单行文本 | 品种信息沟通 | 是 |
| 备注 | 多行文本 | 首次拜访，请协助确认入院流程 | 否 |

点击“提交预约”后进入预约详情页，不直接显示复杂的成功弹窗。

### 4.3 预约详情页

详情页是整个演示的主页面，包含：

- 医院、科室、预约时间和拜访事项；
- 当前状态标签；
- 当前步骤进度条；
- 主操作按钮；
- 操作记录。

建议使用纵向步骤表现流程：

```text
预约信息已填写
        ↓
等待医院确认
        ↓
预约小程序已同意
        ↓
已同步到移动端
```

## 5. 核心演示流程

### 5.1 流程一：发起预约

1. 在移动端首页点击“新建医院预约”。
2. 填写或选择示例预约信息。
3. 点击“提交预约”。
4. 页面进入预约详情。
5. 状态显示为“待医院确认”。
6. 主按钮显示“发送预约小程序转发许可”。

提交后使用页面内的状态更新和轻提示，例如：

> 预约信息已保存，可以发送给医院确认。

不使用阻塞式确认弹窗。

### 5.2 流程二：发送预约小程序转发许可

1. 点击“发送预约小程序转发许可”。
2. 按钮进入短暂处理中状态：`正在生成预约邀请…`。
3. 约 500～800ms 后，展示一个轻量分享卡片。
4. 分享卡片包含医院名称、预约日期、科室和拜访事项。
5. 主按钮变为“已发送，等待医院同意”。
6. 状态变为“待医院确认”。

分享卡片不需要展示真实二维码或真实转发能力，可使用小程序风格的邀请卡片模拟。

### 5.3 流程三：医院侧同意

分享卡片中提供演示用的“切换到医院确认页”入口。该入口只用于演示，不代表真实账号切换。

医院确认页包含：

- 医院名称；
- 代表姓名；
- 预约时间；
- 拜访事项；
- 简短提示：“确认后，预约结果将同步给医药代表。”；
- 主按钮：“同意预约”；
- 次按钮：“暂不同意”。

演示主路径点击“同意预约”：

1. 按钮显示 `正在确认…`；
2. 页面显示确认成功反馈；
3. 状态变为“医院已同意”；
4. 主按钮变为“同步到医药代表移动端”。

“暂不同意”只需展示一条轻提示，不进入复杂驳回流程：

> 本次演示未进入拒绝流程，预约仍等待确认。

### 5.4 流程四：同步回医药代表移动端

1. 点击“同步到医药代表移动端”。
2. 按钮显示 `正在同步预约…`，持续约 600～1000ms。
3. 状态短暂显示“同步中”。
4. 同步完成后显示“已同步”。
5. 页面顶部出现绿色轻提示：

   > 预约已同步到医药代表移动端。

6. 提供“返回我的预约”按钮，返回后预约卡片显示“已确认”。

同步完成后，移动端预约卡片展示：

- 已确认标签；
- 医院名称；
- 预约日期和时间；
- “医院已同意”辅助信息；
- 最近同步时间。

## 6. 状态设计

### 6.1 状态定义

| 状态 | 含义 | 可执行操作 | 展示建议 |
|---|---|---|---|
| 草稿 | 预约信息已填写但尚未提交 | 提交预约、继续编辑 | 中性灰 |
| 待医院确认 | 已发送预约小程序转发许可，等待医院操作 | 查看邀请、重新发送 | 橙色提示 |
| 医院已同意 | 医院已完成确认，等待同步 | 同步到移动端 | 蓝绿色 |
| 同步中 | 正在模拟同步过程 | 暂无 | 蓝色处理中 |
| 已同步 | 预约结果已回到医药代表移动端 | 查看预约、撤销预约 | 绿色成功 |
| 已撤销 | 预约已取消，不再参与后续演示 | 重新发起预约 | 灰色 |

“同步中”属于交互过程状态，不作为正式业务状态长期保存。

### 6.2 状态转换

```text
草稿
  └─提交预约→ 待医院确认
                  └─医院同意→ 医院已同意
                                  └─开始同步→ 同步中
                                                └─同步完成→ 已同步

待医院确认 / 医院已同意 / 已同步
  └─撤销预约→ 已撤销
```

### 6.3 状态反馈原则

- 状态变化必须同时体现在状态标签、步骤条和主按钮上；
- 不只依赖 Toast 告诉用户结果；
- 处理中按钮要保留原按钮位置，避免页面跳动；
- 成功后主操作自然变更，不重复出现“发送”按钮；
- 每次状态变化只出现一条简短提示，不连续弹多个提示框。

## 7. 演示控制能力

为了保证演示稳定，在移动端右上角增加一个低干扰的“演示控制”入口，默认收起，不出现在主流程中。

支持：

- 重置本次演示；
- 直接跳到“待医院确认”；
- 直接跳到“医院已同意”；
- 直接跳到“已同步”；
- 查看当前状态。

“直接跳转”只用于演示人员救场，不替代主流程。重置前需要二次确认，确认文案为：

> 重置后，本次预约记录将恢复为初始状态。

## 8. 数据与实现要求

### 8.1 本地数据模型

```ts
interface DemoAppointment {
  id: string;
  representativeName: string;
  hospital: string;
  department: string;
  appointmentDate: string;
  appointmentTime: string;
  subject: string;
  remark?: string;
  status: '草稿' | '待医院确认' | '医院已同意' | '同步中' | '已同步' | '已撤销';
  sharedAt?: string;
  agreedAt?: string;
  syncedAt?: string;
}
```

### 8.2 状态共享

移动端、医院确认页和演示控制入口必须读取同一份 React 状态，不能分别维护三份状态。

如果页面切换会导致组件卸载，使用 `sessionStorage` 保存本次演示数据，保证返回时状态不丢失。点击“重置本次演示”时清理该数据。

### 8.3 动画与反馈

- 页面切换：180～240ms；
- 按钮处理中：500～1000ms 的模拟延迟；
- 状态标签：使用颜色和文字同步变化；
- 禁止无限转圈；
- 所有按钮在处理中禁用，防止重复触发；
- 移动端按钮触控区域不小于 44px；
- 动画遵守 `prefers-reduced-motion`。

### 8.4 异常处理

演示版只处理必要异常：

- 必填项未填写：在字段下方显示明确提示，不弹通用错误框；
- 重复点击：按钮进入处理中并保持禁用；
- 返回后台：保留当前演示状态；
- 刷新页面：从 `sessionStorage` 恢复，恢复失败时使用默认示例数据。

不模拟网络失败、权限失败和接口超时，避免干扰主演示。

## 9. 文案要求

文案要像真实产品状态，不使用技术实现语言。

| 场景 | 推荐文案 | 避免文案 |
|---|---|---|
| 发送中 | 正在生成预约邀请… | 调用接口中 |
| 等待确认 | 已发送，等待医院确认 | waiting |
| 同意完成 | 医院已同意预约 | 回调成功 |
| 同步中 | 正在同步预约… | 同步接口处理中 |
| 同步完成 | 预约已同步到医药代表移动端 | 数据写入成功 |
| 重置 | 重置本次演示 | 清空数据 |

## 10. 验收标准

### 10.1 入口与切换

- [ ] 现有“演示账号”菜单中可看到“医药代表移动端”；
- [ ] 点击后可进入手机外框页面；
- [ ] 切换不改变当前登录角色和后台权限；
- [ ] 可返回后台，且预约状态不丢失；
- [ ] 切换过程无白屏、无明显跳闪。

### 10.2 主流程

- [ ] 可以新建一条医院预约；
- [ ] 可以发送预约小程序转发许可；
- [ ] 可以进入医院确认页并点击“同意预约”；
- [ ] 可以模拟同步回医药代表移动端；
- [ ] 返回预约列表后，卡片显示“已确认”和同步时间；
- [ ] 状态标签、步骤条、主按钮始终保持一致。

### 10.3 演示体验

- [ ] 主流程无需刷新页面；
- [ ] 不出现技术错误文案；
- [ ] 不出现连续弹窗；
- [ ] 处理中不可重复点击；
- [ ] 演示控制可以重置流程；
- [ ] 从任一关键节点返回后仍可继续演示。

## 11. 开发拆分建议

### 前端

1. 在现有演示账号菜单增加“演示场景”分组和入口。
2. 新增移动端演示容器和手机外框布局。
3. 新增预约首页、新建预约、预约详情、医院确认四个视图。
4. 增加预约状态机和统一状态共享。
5. 增加 Toast、步骤条、按钮处理中和重置能力。
6. 完成移动端演示场景与现有桌面端之间的返回切换。

### 验收

以“从新建预约开始完整走通一次”为主验收路径，再验证中途返回、重复点击和重置三种情况。

## 12. 版本边界

本版本交付一个稳定、可重复播放的演示闭环。后续如进入真实产品开发，再单独拆分：医院端账号、预约小程序、转发授权、回调鉴权、幂等处理、消息通知、预约冲突和审计记录等正式需求。
