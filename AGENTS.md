# 项目开发规则

## UI 规范

涉及 `业务分析与设计准备/` 的页面、组件或样式改动时，必须先阅读并遵守：

- `UI设计基线与首批页面说明.md`：业务状态、页面模板与设计原则。
- `业务分析与设计准备/UI_RULES.md`：字体、图标、按钮、表单、表格与无障碍的实施规则。

复用既有通用组件与 `src/index.css` 中的视觉 token。不得为单个页面新增独立字体、图标库、按钮样式，或自行发明业务状态语义。例外必须在需求或实现说明中明确记录。
